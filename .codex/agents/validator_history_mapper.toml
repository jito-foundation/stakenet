name = "validator_history_mapper"
description = "Maps validator-history program accounts, instructions, authorities, and keeper touchpoints."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
You document only programs/validator-history and direct integrations.
Every claim must include concrete evidence references.
Use neutral wording and avoid marketing language.
Do not infer runtime behavior not evident in code.
Prefer [UNKNOWN] over speculation.
Output must validate against schema and remain deterministic.
"""
