/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { STEWARD_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_STEWARD_ACCOUNTS_DISCRIMINATOR = new Uint8Array([
  172, 171, 212, 186, 90, 10, 181, 24,
]);

export function getCloseStewardAccountsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_STEWARD_ACCOUNTS_DISCRIMINATOR
  );
}

export type CloseStewardAccountsInstruction<
  TProgram extends string = typeof STEWARD_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountStateAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountStateAccount extends string
        ? WritableAccount<TAccountStateAccount>
        : TAccountStateAccount,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CloseStewardAccountsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseStewardAccountsInstructionDataArgs = {};

export function getCloseStewardAccountsInstructionDataEncoder(): FixedSizeEncoder<CloseStewardAccountsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_STEWARD_ACCOUNTS_DISCRIMINATOR,
    })
  );
}

export function getCloseStewardAccountsInstructionDataDecoder(): FixedSizeDecoder<CloseStewardAccountsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseStewardAccountsInstructionDataCodec(): FixedSizeCodec<
  CloseStewardAccountsInstructionDataArgs,
  CloseStewardAccountsInstructionData
> {
  return combineCodec(
    getCloseStewardAccountsInstructionDataEncoder(),
    getCloseStewardAccountsInstructionDataDecoder()
  );
}

export type CloseStewardAccountsInput<
  TAccountConfig extends string = string,
  TAccountStateAccount extends string = string,
  TAccountAuthority extends string = string,
> = {
  config: Address<TAccountConfig>;
  stateAccount: Address<TAccountStateAccount>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getCloseStewardAccountsInstruction<
  TAccountConfig extends string,
  TAccountStateAccount extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STEWARD_PROGRAM_ADDRESS,
>(
  input: CloseStewardAccountsInput<
    TAccountConfig,
    TAccountStateAccount,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): CloseStewardAccountsInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountStateAccount,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STEWARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    stateAccount: { value: input.stateAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.stateAccount),
      getAccountMeta(accounts.authority),
    ],
    programAddress,
    data: getCloseStewardAccountsInstructionDataEncoder().encode({}),
  } as CloseStewardAccountsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountStateAccount,
    TAccountAuthority
  >;

  return instruction;
}

export type ParsedCloseStewardAccountsInstruction<
  TProgram extends string = typeof STEWARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    stateAccount: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: CloseStewardAccountsInstructionData;
};

export function parseCloseStewardAccountsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseStewardAccountsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      stateAccount: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getCloseStewardAccountsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
